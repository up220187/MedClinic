services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    image: "mediclinic-app"
    environment:
      PORT: ${PORT}
      DB_DIALECT: sqlite
      DB_STORAGE: ./data/database.sqlite
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
